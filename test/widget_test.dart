// Smoke test placeholder — app requires async init (DB) so full widget tests
// live in the domain/usecases and presentation/providers directories.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — unit tests are in subdirectories', () {
    expect(true, isTrue);
  });
}
